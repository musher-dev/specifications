specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
  description: Synthetic conformance fixture component.
spec:
  type: SERVICE
  workload:
    source:
      image: nginx:1.29.4-alpine
    endpoints:
      rpc:
        targetPort: 8080
        protocol: GRPC
    health:
      readiness:
        http:
          endpoint: rpc
          path: /healthz
