# A JSON value's string form is a JSON document, so its `default` is a string
# whose content is that document — quoted, or the YAML profile would read it as
# a flow mapping and reject it against `type: string`. Nothing in any phase
# parses it: the contract does not check a value against the shape beside it,
# and that is as true of JSON as of every other member.
specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: nginx:1.29.4-alpine
    endpoints:
      web:
        containerPort: 8080
        protocol: HTTP
        visibility: PRIVATE
  contract:
    inputs:
      settings:
        description: Advanced settings, as a JSON document merged over the defaults.
        schema:
          type: JSON
          default: '{"logLevel":"info"}'
    outputs:
      topology:
        description: Cluster topology, as a JSON document.
        schema:
          type: JSON
        valueFrom: DECLARED
        value: '{"shards":1}'
