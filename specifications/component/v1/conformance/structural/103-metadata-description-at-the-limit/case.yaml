specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
  description: "Web server fixture whose description is exactly the limit long. Web server fixture whose description is exactly the limit long. Web server fixture whose description is exactly the limit long. Web server fixture whose description is exactly the limit long. Web server fixture whos."
spec:
  type: SERVICE
  workload:
    source:
      image: nginx:1.29.4-alpine
    endpoints:
      web:
        targetPort: 8080
        protocol: HTTP
