{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specifications.musher.dev/listing/v1/listing",
  "title": "Musher Listing Document",
  "description": "The catalog storefront entry for a blueprint or component.",
  "$comment": "Structural and type validation only — cross-document reference resolution, slug/directory agreement, and platform capability checks are separate validation layers. See spec.md.",
  "additionalProperties": false,
  "properties": {
    "kind": {
      "const": "LISTING",
      "description": "Document kind discriminator. Must be LISTING.",
      "type": "string"
    },
    "metadata": {
      "$ref": "#/$defs/ListingMetadata"
    },
    "spec": {
      "$ref": "#/$defs/ListingSpec"
    },
    "specVersion": {
      "description": "Version of the Musher specification this document is written against.",
      "enum": [
        "v1"
      ],
      "type": "string"
    }
  },
  "required": [
    "specVersion",
    "kind",
    "metadata",
    "spec"
  ],
  "type": "object",
  "$defs": {
    "ListingMetadata": {
      "additionalProperties": false,
      "description": "Identity block of a listing document. A listing names the item it presents and carries no revision of its own; the item's revision is the blueprint's. See spec.md §3.",
      "properties": {
        "slug": {
          "description": "Item slug — must equal the item's directory name.",
          "type": "string",
          "x-musher-grammar": "label"
        }
      },
      "required": [
        "slug"
      ],
      "type": "object"
    },
    "ListingScreenshot": {
      "additionalProperties": false,
      "description": "One storefront screenshot declared as a relative media path.",
      "properties": {
        "caption": {
          "description": "Accessible caption shown beneath the screenshot. Omitted when none.",
          "maxLength": 280,
          "type": "string"
        },
        "file": {
          "$comment": "Lookahead-free so it compiles under RE2 as well as ECMA-262. A segment must begin with a letter or digit, which is how `.` and `..` are excluded without a negative lookahead; the extension alternation is spelled in character classes because a JSON Schema pattern carries no case-insensitive flag. See spec.md §5.",
          "description": "Path of the screenshot asset under the item's media/ directory, relative to the item root. Must be a .png, .jpg, .jpeg or .webp below media/.",
          "maxLength": 512,
          "minLength": 1,
          "pattern": "^media/(?:[A-Za-z0-9][A-Za-z0-9._-]*/)*[A-Za-z0-9][A-Za-z0-9._-]*\\.(?:[Pp][Nn][Gg]|[Jj][Pp][Gg]|[Jj][Pp][Ee][Gg]|[Ww][Ee][Bb][Pp])$",
          "type": "string"
        }
      },
      "required": [
        "file"
      ],
      "type": "object"
    },
    "ListingSpec": {
      "additionalProperties": false,
      "description": "The `spec` block of a listing document — how the item is presented,\ncategorised, and discovered on the storefront.\n\nIdentity lives in `metadata`, not here. `icon` and `screenshots[].file` are\nmedia paths, resolved inside the item root and required to begin with\n`media/`.",
      "properties": {
        "category": {
          "description": "Storefront category the listing is browsed under.",
          "enum": [
            "MEDIA",
            "PRODUCTIVITY",
            "AI_ML",
            "AGENTS",
            "DEVELOPER_TOOLS",
            "INFRASTRUCTURE",
            "COMMUNICATION",
            "AUTOMATION",
            "NETWORKING",
            "STORAGE",
            "SECURITY",
            "GAMING"
          ],
          "type": "string"
        },
        "description": {
          "description": "Long-form Markdown storefront description. Omitted when none.",
          "maxLength": 20000,
          "type": "string"
        },
        "displayName": {
          "description": "Storefront title shown to prospective installers.",
          "maxLength": 200,
          "minLength": 1,
          "type": "string"
        },
        "homepageURL": {
          "description": "Project homepage URL. Omitted when none. Must use the https, http, or mailto scheme, in any case.",
          "$comment": "Anchored and lookahead-free, so it compiles under RE2 as well as ECMA-262 and cannot be driven to backtrack. The scheme set is spec.md §4.1's, applied to a field that carries a URL directly rather than inside Markdown. The scheme alternation is spelled in character classes because a JSON Schema pattern carries no case-insensitive flag, and RFC 3986 §3.1 makes the scheme case-insensitive — the same reason the media path alternation is spelled that way. Without it this pattern would reject an upper-case scheme the description half of the same rule accepts.",
          "maxLength": 2048,
          "pattern": "^(?:[Hh][Tt][Tt][Pp][Ss]?://|[Mm][Aa][Ii][Ll][Tt][Oo]:)[^\\s]+$",
          "type": "string"
        },
        "icon": {
          "description": "Path of the listing's icon under the item's media/ directory (e.g. media/icon.png), relative to the item root. Omitted for no icon.",
          "$comment": "The same grammar ListingScreenshot.file carries; see the note there.",
          "maxLength": 512,
          "pattern": "^media/(?:[A-Za-z0-9][A-Za-z0-9._-]*/)*[A-Za-z0-9][A-Za-z0-9._-]*\\.(?:[Pp][Nn][Gg]|[Jj][Pp][Gg]|[Jj][Pp][Ee][Gg]|[Ww][Ee][Bb][Pp])$",
          "type": "string"
        },
        "itemType": {
          "$comment": "spec.md §3. BLUEPRINT if and only if the item root holds blueprint.yaml — a semantic rule, because it reads the item root rather than the document.",
          "description": "Shape of the catalog item this listing presents: BLUEPRINT (a composition) or COMPONENT (a single building block).",
          "enum": [
            "BLUEPRINT",
            "COMPONENT"
          ],
          "type": "string"
        },
        "license": {
          "$comment": "spec.md §4 / ADR 0003 §2 and §4. The shape is this contract's and the membership is SPDX's, published at https://spdx.org/licenses/ and deliberately not restated. Flat: a parenthesised expression is outside this grammar, which spec.md §9 records. Anchored and lookahead-free, so it compiles under RE2 as well as ECMA-262.",
          "description": "SPDX licence expression for the listed software, such as MIT or Apache-2.0 WITH LLVM-exception. Omitted when unspecified.",
          "maxLength": 256,
          "pattern": "^[A-Za-z0-9.+-]{1,64}(?: (?:WITH|AND|OR) [A-Za-z0-9.+-]{1,64}){0,8}$",
          "type": "string"
        },
        "lifecycleStage": {
          "description": "Maturity stage shown on the storefront: STABLE, BETA, EXPERIMENTAL, or SUNSET.",
          "enum": [
            "STABLE",
            "BETA",
            "EXPERIMENTAL",
            "SUNSET"
          ],
          "type": "string"
        },
        "screenshots": {
          "description": "Storefront screenshots in display order, declared as media paths. At most twelve.",
          "items": {
            "$ref": "#/$defs/ListingScreenshot"
          },
          "maxItems": 12,
          "type": "array"
        },
        "sourceRepoURL": {
          "description": "Source repository URL. Omitted when none. Must use the https, http, or mailto scheme, in any case.",
          "$comment": "The same grammar homepageURL carries; see the note there.",
          "maxLength": 2048,
          "pattern": "^(?:[Hh][Tt][Tt][Pp][Ss]?://|[Mm][Aa][Ii][Ll][Tt][Oo]:)[^\\s]+$",
          "type": "string"
        },
        "summary": {
          "description": "One-line storefront tagline summarising the listing.",
          "maxLength": 280,
          "minLength": 1,
          "type": "string"
        },
        "supportURL": {
          "description": "Support or documentation URL. Omitted when none. Must use the https, http, or mailto scheme, in any case.",
          "$comment": "The same grammar homepageURL carries; see the note there. mailto: is in the set for this field in particular — a support address is a plausible value.",
          "maxLength": 2048,
          "pattern": "^(?:[Hh][Tt][Tt][Pp][Ss]?://|[Mm][Aa][Ii][Ll][Tt][Oo]:)[^\\s]+$",
          "type": "string"
        },
        "tags": {
          "description": "Free-form discovery tags applied to the listing. At most sixteen, and a tag may not repeat.",
          "items": {
            "$comment": "spec.md §4 / ADR 0007 §4. A tag is lowercase kebab and is compared after normalisation, so the grammar is what makes two spellings of one tag impossible rather than merely discouraged.",
            "maxLength": 40,
            "pattern": "^[a-z0-9][a-z0-9-]{0,38}[a-z0-9]$",
            "type": "string"
          },
          "maxItems": 16,
          "type": "array",
          "uniqueItems": true
        }
      },
      "required": [
        "itemType",
        "displayName",
        "summary",
        "category",
        "lifecycleStage"
      ],
      "type": "object"
    }
  }
}
