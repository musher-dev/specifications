specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
  description: Synthetic conformance fixture component.
spec:
  type: SERVICE
  workload:
    source:
      image: example/web:1
    endpoints:
      web:
        targetPort: 8080
        protocol: HTTP
    health:
      readiness:
        http:
          endpoint: web
          path: /
          auth:
            basic:
              username:
                input: adminUser
              password:
                input: vncPassword
  contract:
    inputs:
      adminUser:
        description: Synthetic fixture input.
        schema:
          type: string
        target:
          envVarKey: ADMIN_USER
        default: admin
      vncPassword:
        description: Synthetic fixture input.
        schema:
          type: string
        target:
          envVarKey: VNC_PW
        sensitive: true
