# Every format member names a lexical convention for text, so the field refines
# a STRING and nothing else. URI on a NUMBER describes nothing; that it used to
# validate was an accident of the two fields never having been described
# together.
specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: nginx:1.29.4-alpine
    endpoints:
      web:
        containerPort: 8080
        protocol: HTTP
        visibility: PRIVATE
  contract:
    inputs:
      retries:
        description: Number of times a failed request is retried.
        schema:
          type: NUMBER
          format: URI
    outputs: {}
