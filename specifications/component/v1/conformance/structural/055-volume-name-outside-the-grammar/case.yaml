specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: postgres:17.10-alpine
    endpoints:
      primary:
        containerPort: 5432
        protocol: TCP
    volumes:
      Data_Volume:
        mountPath: /var/lib/postgresql/data
        minimumSizeGiB: 10
