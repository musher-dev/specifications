# This file begins with a UTF-8 byte order mark. Editors on Windows add one
# without being asked, so rejecting it would fail documents whose only fault is
# the tool that saved them. It is ignored rather than tolerated: nothing about
# the document's meaning changes.
specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
  description: Static web server.
spec:
  type: SERVICE
  workload:
    source:
      image: nginx:1.29.4-alpine
    endpoints:
      web:
        targetPort: 8080
        protocol: HTTP
