specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: WORKER
    command: /bin/consume
    source:
      type: IMAGE
      ref: ghcr.io/musher-dev/worker:1.4.0
    endpoints:
      web:
        containerPort: 8080
        protocol: HTTP
