specVersion: v1
kind: BLUEPRINT
metadata:
  slug: acme-wiki
  revision: 1
spec:
  components:
    models:
      componentRef: ./components/models.yaml
      size: null
      bindings:
        apiKey:
          type: PARAMETER
          parameter: apiKey
        baseUrl:
          type: OUTPUT
          node: models
          output: installDefault
