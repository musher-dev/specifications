specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: ghcr.io/musher-dev/payments:1.4.2
    endpoints:
      web:
        containerPort: 8080
        protocol: HTTP
  contract:
    inputs:
      apiKey:
        schema:
          type: string
        description: Credential this service authenticates with.
        sensitive: true
        target:
          envVarKey: API_KEY
    outputs: {}
