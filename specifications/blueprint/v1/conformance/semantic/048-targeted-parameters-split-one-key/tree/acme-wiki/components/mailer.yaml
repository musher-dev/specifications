specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: ghcr.io/musher-dev/mailer:1.4.2
    endpoints:
      web:
        containerPort: 8080
        protocol: HTTP
        visibility: PUBLIC
  contract:
    inputs:
      apiKey:
        schema:
          type: STRING
          sensitive: true
        description: Credential this service authenticates with.
    outputs: {}
