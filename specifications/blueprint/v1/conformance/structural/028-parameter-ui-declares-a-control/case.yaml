specVersion: v1
kind: BLUEPRINT
metadata:
  slug: acme-wiki
  revision: 1
spec:
  components:
    db:
      componentRef: ./components/postgres.yaml
      size: general.standard.small
      bindings:
        backend:
          type: PARAMETER
          parameter: backend
  parameters:
    backend:
      ui:
        label: Storage backend
        widget: SELECT
