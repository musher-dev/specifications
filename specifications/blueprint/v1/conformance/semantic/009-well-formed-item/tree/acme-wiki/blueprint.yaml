# Every item-scoped rule satisfied at once, which is what makes this the
# regression pin: a careless implementation of any one of them rejects a
# well-formed item, and nothing else in the corpus would notice.
#
# The slug is the directory name, the sibling listing names the same item, both
# component documents are referenced, both references resolve inside the item,
# the connection names an output `db` really publishes and an input `web`
# really declares, the two ends of that wire agree on type and resourceType,
# and `adminPassword` is required on both nodes, declared identically, and
# covered by one generated parameter whose inputs are both sensitive.
specVersion: v1
kind: BLUEPRINT
metadata:
  slug: acme-wiki
  revision: 1
spec:
  components:
    db:
      componentRef: ./components/postgres.yaml
      size: general.standard.small
      connections: {}
    web:
      componentRef: ./components/web.yaml
      size: general.standard.small
      connections:
        databaseUrl:
          fromRole: db
          fromOutput: connectionString
  parameters:
    adminPassword:
      generator:
        byteLength: 32
        encoding: HEX
      ui:
        label: Administrator password
