specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: postgres:17.10-alpine
    endpoints:
      primary:
        containerPort: 5432
        protocol: TCP
  contract:
    inputs:
      backend:
        schema:
          type: string
          enum:
            - S3
            - AZURE
        description: Object store the database ships its write-ahead log to.
        target:
          envVarKey: BACKEND
    outputs: {}
