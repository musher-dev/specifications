specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    endpoints:
      web:
        containerPort: 8080
        protocol: HTTP
    terminationGracePeriodSeconds: 30
    source:
      type: IMAGE
      ref: nginx:1.29.4-alpine
