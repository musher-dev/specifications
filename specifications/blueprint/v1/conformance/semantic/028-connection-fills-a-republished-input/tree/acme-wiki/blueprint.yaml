specVersion: v1
kind: BLUEPRINT
metadata:
  slug: acme-wiki
  revision: 1
spec:
  components:
    models:
      componentRef: ./components/models.yaml
      size: null
      bindings:
        baseUrl:
          type: OUTPUT
          node: web
          output: publicUrl
        apiKey:
          type: PARAMETER
          parameter: apiKey
    web:
      componentRef: ./components/web.yaml
      size: general.standard.small
      bindings:
        llmBaseUrl:
          type: OUTPUT
          node: models
          output: baseUrl
      exposure:
        web: PUBLIC
      volumes: {}
  parameters:
    apiKey:
      ui:
        label: API key
