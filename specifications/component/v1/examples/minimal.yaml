specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
  description: Static web server with one HTTP endpoint.
spec:
  type: SERVICE
  workload:
    source:
      image: nginx:1.29.4-alpine
    endpoints:
      web:
        targetPort: 8080
        protocol: HTTP
