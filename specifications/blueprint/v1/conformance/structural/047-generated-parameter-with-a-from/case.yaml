specVersion: v1
kind: BLUEPRINT
metadata:
  slug: app
  revision: 1
  description: Synthetic conformance fixture blueprint.
spec:
  parameters:
    key:
      generator: {}
      from: "${{ variables.app.key }}"
  components:
    web:
      componentRef: ./components/web.yaml
      compute:
        profile: general.standard.small
      bindings:
        key:
          parameter: key
