# yaml-language-server: $schema=https://specifications.musher.dev/component/v1/component.schema.json
#
# A stateful service exercising the full surface: a private endpoint, literal
# and generated environment variables, a persistent volume, health probes, and
# a configuration contract with a sensitive input. The component declares that
# it needs a password; the blueprint deploying it decides who supplies one.
specVersion: v1
kind: COMPONENT
metadata:
  revision: 1
spec:
  workload:
    type: SERVICE
    source:
      type: IMAGE
      ref: postgres:17.10-alpine
    endpoints:
      primary:
        containerPort: 5432
        protocol: TCP
        visibility: PRIVATE
    envVars:
      - key: PGDATA
        value:
          type: LITERAL
          value: /var/lib/postgresql/data/pgdata
      - key: POSTGRES_DB
        value:
          type: LITERAL
          value: app
    volumes:
      data:
        sizeGiB: 10
        mountPath: /var/lib/postgresql/data
  contract:
    inputs:
      postgresPassword:
        description: Password the database superuser is created with.
        schema:
          type: STRING
          sensitive: true
        required: true
        target:
          envVarKey: POSTGRES_PASSWORD
    outputs: {}
