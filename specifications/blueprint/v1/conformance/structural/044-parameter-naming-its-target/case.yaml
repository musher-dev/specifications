# Both narrowing fields are OPTIONAL and take the grammars of what they name: a
# node name, and an input name. Which node and which input exist is §5.1's
# semantic question and not this case's.
specVersion: v1
kind: BLUEPRINT
metadata:
  slug: acme-wiki
  revision: 1
spec:
  components:
    payments:
      componentRef: ./components/payments.yaml
      size: general.standard.small
      connections: {}
  parameters:
    stripeKey:
      toNode: payments
      toInput: apiKey
      ui:
        label: Stripe API key
